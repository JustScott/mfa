# Maintainer: Scott Wyman <development@scottwyman.me>
pkgname='mfa'
pkgver=0.1.0  # Update this with your package version
pkgrel=1    # Increment this when you update the package
pkgdesc='A simple TOTP MFA CLI authenticator'
arch=('x86_64')
url="https://github.com/JustScott/mfa"
license=('GPLv3')
depends=(python python-typer python-keyring python-pyotp python-psutil)
makedepends=()
checkdepends=()
optdepends=()
provides=()
conflicts=()
replaces=()
backup=()
options=()
install=
changelog=
source=("$pkgname-$pkgver.tar.gz"
        "$pkgname-$pkgver.patch")
noextract=()
md5sums=()
validpgpkeys=()

package() {
}
